version: "3.9"

services:
  api:
    build: .
    container_name: fastapi
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - database_default
    restart: always

networks:
  database_default:
    external: true